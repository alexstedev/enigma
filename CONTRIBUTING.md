# Contributing Guidelines and Development

## Development Setup

To run locally, clone the repository, install the dependencies and run the development server:
```bash
npm install
npm run dev
```

To build the project for production, run:
```bash
npm run build
```

